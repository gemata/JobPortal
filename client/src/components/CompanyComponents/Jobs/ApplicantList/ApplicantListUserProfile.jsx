import React, { useState } from 'react';


const ApplicantListUserProfile = ({ id }) => {
    // Logic to fetch and display user profile based on the id
    return (
        <div>
            <p>User ID: {id}</p>
            {/* Add more details and components as needed */}
        </div>
    );
};

export default ApplicantListUserProfile;
